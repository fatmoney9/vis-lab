# 可视化规范站 · 工作流

> 项目目标：把设计稿中的可视化规范迁移为可交互的规范站（living style guide）。
> 本文件是整个项目的权威工作规则：后续所有的拆分、拼接、demo 生成都必须遵守这里的约定。

---

## 一、总原则：规范只有一个家

每条规范在仓库里只有一份权威表述。组件代码、文档页、demo 只能**引用或派生**它，
不得复述。复述会漂移，漂移是规范站失去公信力的根本原因。

一条具体的检验标准：**任何一条规范变更时，代码里只应该改一个地方。**

---

## 二、技术路线（已定稿）

| 决策点 | 选择 | 理由 |
|---|---|---|
| 计算与 DOM 辅助 | D3（比例尺/刻度/路径生成 + `d3-selection` 装配 SVG） | 不使用 D3 的高层图表封装，数学、结构与样式仍由项目控制 |
| 渲染层 | 项目代码显式装配 SVG | 真实 DOM：CSS 变量直达、可挂事件、矢量不糊 |
| 样式注入 | CSS 变量（design tokens） | 改 token 全站生效，暗色模式 = 换一组变量 |
| 不采用 | 纯 CSS（画不了折线/算不了刻度）、ECharts（黑盒，样式走不了 token 链路）、canvas（本项目数据量用不上，且 CSS 够不着画布内部） | |
| 性能逃生通道 | 若未来出现大数据量组件，仅该组件换 canvas 渲染，比例尺与规范逻辑原样复用（`d3.line().context(ctx)`） | |

---

## 三、分层架构（L0–L3）

依赖方向永远向下：L3 → L2 → L1 → L0。禁止跨层调用与反向依赖。

| 层 | 职责 | 当前权威位置 |
|---|---|---|
| **L0 · Design tokens** | 主题值 JSON、行为配置与系列色板；构建生成 CSS 变量 | `tokens/` |
| **L1 · 共享构件** | 跨图表复用的渲染与计算单元：轴、网格、图例、tooltip、比例尺、格式化等 | `charts/core/` |
| **L2 · 图表组件** | 把 L1 构件按固定方式拼好，只暴露数据 + 语义配置；图表专属计算留在组件内部 | `charts/charts/` |
| **L3 · 规范与预览** | 条目化规范 + 活 demo；独立规范站尚未并入本仓库 | `specs/` + `playground/` |

**粒度判断标准（机械执行，计算逻辑与可见构件同样适用）：**
- 两种以上图表都要遵守的规范 → 沉到 L1（例：刻度算法，柱状图和折线图共用同一份）
- 只属于一种图表的逻辑 → 留在该 L2 组件内部
- 不为"将来可能用到"提前抽象

**依赖链全景：**

```
L3 demo ──只认──▶ L2 图表组件 ──调用──▶ L1 构件（含计算模块）──import──▶ d3（仅计算函数）
                                                └──读取──▶ CSS 变量（L0 tokens）
```

---

## 四、拼接铁律

任何人（包括 AI 会话）在此项目中生成或修改代码，必须遵守：

1. **颜色只来自权威数据源。** 元素色来自 `tokens/<theme>.json` 生成的 CSS 变量；系列色只在
   `tokens/palette.json` 定义，由取色器写入 `--dv-series-N`。生产组件源码内禁止色值字面量
   （`#hex`、`rgb(`、颜色名）；结构样式集中在 `charts/styles.css`。
2. **系列颜色按固定槽位顺序分配，不接受配置。** 图表 API 没有 color 参数（有意设计）；
   图例显隐、数据过滤都不重排颜色——颜色跟随实体，不跟随排名。
3. **demo 不拼装。** L3 的 demo 只传"数据 + 少量语义配置"调用 L2 组件。
   在 demo 或文档里手写 SVG、手调间距 = 违规。拼装逻辑是规范的一部分，必须固化在 L2。
4. **组件 API 不暴露样式参数。** 只收数据和语义配置（platform、unit、initialZoom 等）。
   想改样式 = 改 token 或修订规范条目，不是给 API 加参数。
5. **每条规范有稳定 ID**（如 `MARK-01`、`AXIS-02`），权威定义写在规范文档里；
   实现处的代码注释必须引用 ID——**计算代码同样如此**（如"固定 5 条网格线"的
   刻度函数标注 `SCALE-01`）。修订规范时全库搜索 ID 定位所有实现点。
6. **D3 等第三方依赖只允许 charts 包（L1/L2）引用。** L3 的 demo 和文档站
   永不直接 import D3——demo 拿不到比例尺等原材料，违规拼装就没有原料。
7. **AI 的角色边界：** AI 负责按规范 ID 实现/修改 L1、L2 模块、用组件 API 写 demo；
   不允许 AI 在生成 demo 时从原子构件临场拼接图表（非确定性输出会破坏一致性）。

---

## 五、规范条目的组织

- 规范正文用 markdown 写，按主题分文件（color / axis / tooltip / 每种图表一篇）
- 每条规则一个稳定 ID，格式 `{类别}-{序号}`：
  `COLOR-` 颜色 · `SCALE-` 比例尺刻度 · `GRID-` 网格 · `AXIS-` 坐标轴 · `DATAZOOM-` 缩放轴 ·
  `MARK-` 图形标记 · `LEGEND-` 图例 · `TEXT-` 文本 · `TOOLTIP-` 浮层 · `MOTION-` 动效
- 每条规范页的标准结构：规则表（ID + 描述）→ 活 demo → Do/Don't 对比 → API 说明

---

## 六、主题与多端机制

主题差异分**两类，走两条通道，共用一个开关**：

| 通道 | 内容 | 载体 | 消费方式 |
|---|---|---|---|
| **值 token** | 颜色、字号、间距、线宽等数值 | `tokens/<theme>.json` → CSS 变量 | 渲染引擎直接消费（CSS / `getComputedStyle`） |
| **形态/行为** | 布局形式（Y 轴 A/B）、轴位置、碰撞策略等**参与逻辑分支和几何计算的规范** | `tokens/behavior.json` → `core/theme.js` 解析 | L2 组件取出后作为参数传给 L1 |

- **共用开关**：容器（或任意祖先）上的 `data-theme` 属性同时作为两条通道的主题输入。
  值 token 会随属性经 CSS 级联更新；形态/行为只在组件重绘时重新解析，因此切主题必须
  「更新属性 → 触发 L2 重绘」。当前 Preview 通过重建图表保证同步；仅改属性会造成值已切换、
  SVG 几何仍保留旧行为。作用域可挂任意容器，同页可并排对比多主题。
- **L1 保持主题无关**：L1 只提供参数化能力（能画 A 也能画 B），永远不出现
  `if (theme === 'x')`；"哪个主题用哪种形态"只存在于 behavior.json 一处。
- **形态参数不暴露给使用者**：demo 只传数据 + platform，形态由 L2 从
  `resolveBehavior(host, platform)` 解析——布局是主题规范定的，不是使用者偏好。
- **为什么形态不塞进 CSS 变量**：形态值要参与几何计算和分支逻辑，放 CSS 里
  无法类型校验、无法表达端分叉嵌套、调试隐形。CSS 变量只装"渲染引擎直接消费的值"。
- **一个主题 = 一份完整的 token 文件**（当前主题即事实上的 base，不设抽象底座）。
  构建输出成作用域化的 CSS 变量；若需要暗色模式，暗色值作为同名变量的第二组取值
  （如 `[data-theme="dark"]`）一并维护。
- **不变的底线约定：组件只读 CSS 变量与 behavior 解析结果，永远不关心值从哪来。**
  守住这条，token 文件怎么组织都可以随时调整，组件一行不用改。
- **多主题**：每个主题一份完整 token 文件，构建输出到各自的作用域
  （`[data-theme="x"]`）；组件源码不因主题复制，但运行时切主题必须改属性并触发重绘；
  作用域可挂任意容器，同页可并排对比多主题。
- **多主题的唯一硬约束：所有主题必须覆盖同一套变量名**（变量名清单即主题间的合同）。
  多主题出现时，构建脚本须校验各主题 key 集合与基准一致，缺 key 即构建失败。
  **behavior.json 适用同一条合同纪律**（`core/theme.js` 加载时校验，缺键抛错）。
- 组织方式（各自全量 vs base + delta 合并）只是维护成本取舍，届时再定
  ——那只是 JSON 的重新组织，不需要今天预付成本。图表组件本身不含任何主题逻辑。
- **多端（PC / App）是组件的一个配置维度**（`platform` 参数），不是两套代码。
  端差异（轴标签形式、字号、图表宽度）在 L1 构件内部消化。
- 换入品牌色板前，先跑色觉安全校验（相邻色 CVD 差值、对比度、亮度带），不靠目测。

---

## 七、日常工作流

### 迭代单位：一条规范条目

```
改规范文字（specs/*.md，定 ID）
  → 改对应的一个模块（L0 token / L1 构件 / L2 组件）
  → 亮暗两种模式下目检 demo
  → （接入后）视觉回归对账
```

### 新增一种图表

1. 在规范文档写该图表的规范条目，分配新 ID
2. 盘点所需构件：L1 有则复用；通用且缺失则新建 L1 模块
3. 写 L2 组件，代码注释引用规范 ID
4. 写 demo（只传数据），嵌入规范页
5. 亮/暗两种外观下目检，确认 token 链路贯通

### 从现有产品反抽参数（需要时）

优先级：① 图表库运行时实例直接要配置（如 `echarts.getInstanceByDom(el).getOption()`）
→ ② 给 Canvas API 原型打补丁记录每笔绘制 → ③ 读像素（最后手段）。
轴标签/图例往往是 HTML，直接检查元素。

---

## 八、当前状态与后续里程碑

截至 2026-07-22，当前仓库已完成：三主题 token 构建、L1 轴/网格/图例/tooltip 等共享构件、
`CartesianChart`（柱/堆叠/折线/折柱组合/双 Y/缩放轴 datazoom）、已发布到 GitHub Pages 的多主题 playground，
以及 token 合同、首批纯逻辑单元测试、分层和 Spec ID 提交前守卫。完整测试流程见 `TESTING.md`。

后续按以下顺序推进：

1. **上视觉保险**：Playwright 视觉回归接入 CI；补组件色值字面量 lint 与色板 CVD/对比度校验。
2. **补未完成规范**：图例溢出、数据标签、移动端触摸等以 `specs/*.md` 待办为准（datazoom 已落地 [specs/datazoom.md](specs/datazoom.md)）。
3. **规范站产品化**：决定是否把当前 `specs/` + `playground/` 迁入独立站点；迁移前当前目录就是权威入口。
4. **进阶工具**：token 对照表自动生成与更多图型预览。

---

## 九、质量保障

- **已接入 · 逻辑单测**：格式化、值域、布局、堆叠和系列归一化等纯函数使用 `node:test`，按 Spec ID 覆盖边界
- **待接入 · 视觉回归**：每个 demo 的关键状态（默认 / hover / 图例隐藏 / 暗色）截图存基线，改动逐像素对比
- **待接入 · token lint**：CI 扫描组件源码，出现色值字面量即失败
- **待接入 · 色板校验**：任何新色板/新主题入库前跑 CVD 与对比度校验脚本
- 完整分层、覆盖矩阵、基线审批和失败处理以 `TESTING.md` 为准；未接入项不得标为自动化覆盖
