# 测试流程

本项目的测试目标是保证规范、计算、SVG 结构、交互和最终视觉一致。测试用例不重新定义视觉规则；
规则仍以 `specs/*.md` 的 Spec ID 为权威来源，测试只把规则转成可重复执行的验收证据。

## 一、质量模型

测试按由低到高的层级组织。下层失败时先修下层，不用截图掩盖计算或结构错误。

| 层级 | 验证内容 | 当前状态 | 执行位置 |
|---|---|---|---|
| 静态门禁 | token 合同、生成物、语法、分层、Spec ID 回引 | 已接入 | pre-commit / CI |
| 逻辑单测 | 格式化、值域、布局、堆叠、系列归一化等纯函数 | 已接入首批 | `tests/*.test.mjs` + CI |
| DOM 结构 | SVG 节点、属性、图层顺序、隐藏状态 | 待接入 | Playwright |
| 浏览器交互 | hover、Tooltip、图例、Resize、主题与端切换 | 待接入 | Playwright |
| 视觉回归 | 三主题关键图型与状态的截图差异 | 待接入 | Playwright + 人工审批 |
| 非功能测试 | 可访问性、性能、浏览器兼容性 | 待规则明确后接入 | 浏览器测试 / 专项测试 |

## 二、本地验证命令

单元测试使用 Node.js 内置 `node:test`，不引入 package manager 或第三方依赖。项目源码是浏览器原生
ES Modules，Node 运行 `.js` 模块时需要显式指定默认模块类型：

```sh
node --test tests/*.mjs
```

所有改动至少运行：

```sh
node tokens/build.mjs
node --test tests/*.mjs
sh hooks/lint-layers.sh
node hooks/lint-spec-ids.mjs
```

`node tokens/build.mjs` 会更新生成文件 `tokens/tokens.css`。若只是检查，应在执行后确认生成物没有意外差异。

## 三、单元测试规则

### 测试对象

优先测试无 DOM、无 CSS 计算样式、无网络依赖的确定性逻辑：

- `charts/core/format.js`：主题数值格式边界；
- `charts/charts/cartesian/series.js`：默认值和固定系列槽位；
- `charts/charts/cartesian/layout.js`：分组、单柱、普通堆叠和百分比堆叠；
- `charts/charts/cartesian/domain.js`：单轴值域、隐藏策略、柱线独立累计；
- `charts/core/watermark.js`：水印锚点几何（角 × grid 边 × 偏移，`watermarkAnchor`）。
- `charts/core/axis-title.js`：轴标题带高、贴外缘锚点与同带内主轴优先让位（`axisTitleBand` / `axisTitleAnchor` / `dropCollidingTitles`）。
- `demos/examples.js`：示例声明与图表形态的一致性（双 Y 示例必带 `y2` 等，`buildConfig`）。

依赖浏览器 SVG 测量、CSS token、事件或远程 D3 import 的模块不在 Node 单测中伪造环境；它们进入后续
Playwright 测试。只有能形成稳定、真实合同的逻辑才下沉为单元测试。

### 用例命名与追溯

- 对应明确规范的测试名以 Spec ID 开头，例如 `BAR-05：……`。
- 一个测试只表达一个规则或一个紧密相关的边界集合。
- 测试数据应尽量小，失败时能直接看出原因。
- 修复缺陷时先增加能复现缺陷的测试，再修改实现。
- 不断言内部实现步骤；断言规范可观察结果。

### 必测边界

数值和布局逻辑至少考虑：

- 空数组、单项、多项；
- `0`、负值、正负跨零、`null`、`NaN`；
- 阈值前、阈值本身、阈值后；
- 宽容器、临界宽度、窄容器；
- 全部系列、部分隐藏、全部隐藏；
- 单轴、双轴以及柱线组合；
- 普通堆叠与百分比堆叠。

未定义的产品语义不通过测试擅自定案。例如“归一化正负混合”仍是 BAR-06 待办，在规范确定前只测试
当前明确的正值假设，并在测试名和文档中保持边界清楚。

## 四、浏览器与视觉测试设计

以下部分是下一阶段的接入标准，当前不能在 PR 中勾选为已自动化。

### 浏览器夹具

浏览器测试应调用公开的 `CartesianChart`，只传数据与语义配置。测试夹具不得手写生产 SVG、不得绕过
L2 临场拼装 L1，也不得加入只为截图好看的样式参数。

建议为每个稳定场景提供固定 ID：

- 基础柱：正值、负值、0、null；
- 分组柱：多系列与隐藏重排；
- 普通/百分比堆叠；
- 单折线、多折线、null 断口、高密度点；
- 折柱组合与双 Y；
- 长 X 标签、长图例名、窄容器。

测试数据必须固定，不使用当前时间、随机数、网络业务数据或依赖本机区域设置的文本。

### DOM 与交互断言

浏览器测试优先断言语义和结构，再做截图：

- SVG、网格线、柱、折线和点的数量；
- `null` 不生成图元、0 值占位符合规则；
- 图层顺序和系列固定颜色槽位；
- 图例点击后的可见系列、关闭态和堆叠 refit；
- hover 最近类目、Tooltip 标题/行序/数值；
- 指示线与分组柱 block 的互斥；
- mouseleave 延迟、滚动立即隐藏；
- Resize 后重新布局且不产生重复节点；
- 主题、端和明暗切换后值通道与行为通道同步。

### 视觉覆盖矩阵

不对全部组合做笛卡尔积截图。使用“代表性冒烟 + 规范差异全覆盖”：

| 维度 | 冒烟集 | 完整视觉集 |
|---|---|---|
| 主题 | THS | THS / iFinD-PC / Ainvest |
| 端 | PC | PC / 移动端 |
| 模式 | Light | Light / Dark |
| 图型 | 基础柱、折线、折柱双 Y | 所有稳定分类 |
| 状态 | 默认 | 默认、hover、图例隐藏、窄容器 |

只有主题或端存在规范差异的场景才扩展组合。新增截图前应说明它能捕获哪条既有基线捕获不到的风险。

### 基线更新规则

- 视觉差异默认按失败处理，不能由 CI 自动接受新基线。
- PR 必须附“期望图、实际图、差异图”或测试报告链接。
- 只有规范或 token 的有意变化才能更新基线，并在 PR 中关联 Spec ID。
- 修复测试环境、字体或抗锯齿差异时，不得顺带接受无关视觉变化。
- 基线由固定浏览器版本和固定视口生成；开发者本机截图只作辅助证据。

## 五、按改动类型选择测试

| 改动类型 | 最低验证要求 |
|---|---|
| 规范文字 | Spec ID 回引检查；确认状态与实现一致 |
| token 值 | token 重建；三主题合同；受影响主题 × 端 × 明暗目检；接入后跑视觉回归 |
| 纯计算 | 新增/更新单元测试；相关 Spec ID 边界用例 |
| L1 渲染构件 | 单元测试（适用时）+ DOM 测试 + 受影响视觉基线 |
| L2 编排 | 组合场景 DOM/交互测试 + 冒烟视觉集 |
| 交互行为 | 事件测试、隐藏/清理测试、至少一个边缘位置视觉状态 |
| Preview 外壳 | 页面冒烟；确认没有把生产拼装逻辑放进 L3 |

## 六、PR 与缺陷流程

### 开发阶段

1. 确认或新增 Spec ID，并写清验收条件。
2. 选择受影响测试层，不用视觉截图代替可单测的计算。
3. 先写失败用例或固定浏览器夹具。
4. 实现后运行受影响测试和全部快速门禁。
5. 有视觉变化时附证据，并说明是否需要更新基线。

### 失败处理

- 可稳定复现：作为真实失败处理，修实现或修正规范后再更新测试。
- 仅 CI 失败：先比较 Node/浏览器/字体/视口环境，不直接重跑碰运气。
- 偶发失败：记录复现频率和运行证据；不得长期用重试掩盖。
- 确认不稳定且短期无法修复：建立 Issue、标注负责人和期限后才可临时隔离。
- 禁止删除测试、放宽像素阈值或批量更新基线来绕过未知失败。

### 完成标准

一项能力只有在以下条件满足后才能在规范中标为完成：

- 规范文本、实现和状态一致；
- 适用的自动化测试已通过；
- 尚未自动化的视觉或交互项有人工验证证据；
- 三主题、端、明暗中的适用范围已明确；
- 已知未覆盖边界记录在规范待办或 Issue 中；
- CI 质量门禁通过。

## 七、接入路线

1. 扩充纯逻辑单测：比例尺、双轴和图例选择逻辑在具备稳定 Node 依赖边界后补齐。
2. 引入 Playwright 浏览器夹具，先做 DOM 与交互断言。
3. 在固定浏览器环境建立少量关键视觉基线。
4. 扩展三主题差异矩阵，并增加失败产物归档。
5. 增加色值字面量 lint、色板 CVD/对比度、可访问性和性能专项检查。

路线按风险逐层推进；未接入的层级保持“待接入”，不以人工目检结果冒充自动化覆盖。
